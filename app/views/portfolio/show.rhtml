<%= @project.title %><%= ": за кадром" if params[:show] == 'overs' %>


<%= params[:show] == 'overs' ? @project.overs : @project.body %>

<% content_for :sidebar do %>

<div>
<%= link_to "За кадром", "?show=overs", :class => "overs" if @project.overs %>
</div>
<br clear="all" />

<div id="sidebar">
<%= @project.sidebar if @project.sidebar %>
</div>
<% end %>